import React, { memo } from "react";
import { View, Text } from "react-native";

const OTP: React.FC = () => {
    return (
        <View>
            <Text>OTP</Text>
        </View>
    )
}

export default memo(OTP);